<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier un catway</title>
</head>
<body>
    <h1>Modifier un catway</h1>
    <div>
        <% const msg = typeof message !== 'undefined' ? message : '' %>
        <% if (msg) {  %>
            <p> <%= msg %> </p>
        <% } %>
    </div>
    <form action="/catways/<%= catway._id %>?_method=PUT" method="POST">
        <label for="catwayNumber">Numéro de catway:</label>
        <input type="number" id="catwayNumber" name="catwayNumber" value="<%= catway.catwayNumber %>" required />
        <br>
        <label for="type">Type:</label>
        <select id="type" name="type" value="<%= catway.type %>" required >
            <option value="short" <%= catway.type === 'short' ? 'selected' : '' %>>Court</option>
            <option value="long" <%= catway.type === 'long' ? 'selected' : '' %>>Long</option>
        </select>
        <br>
        <label for="catwayState">État:</label>
        <input type="text" id="catwayState" name="catwayState" value="<%= catway.catwayState %>" required />
        <br>
        <label for="boatName">Nom du bateau:</label>
        <input type="text" id="boatName" name="boatName" value="<%= catway.boatName %>" required />
        <br>
        <button type="submit">Enregistrer les modifications</button>
    </form>
    <a href="/catways">Liste des catways</a><br>
    <a href="/dashboard">Accéder au tableau de bord</a>
</body>
</html>
